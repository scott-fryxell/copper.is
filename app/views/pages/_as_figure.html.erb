<%-cache page do-%>
<figure <%=page.as_item_attributes%> >
  <%-if page.thumbnail_url-%>
    <img itemprop="thumbnail_url" src="<%=page.thumbnail_url%>">
  <%-else-%>
    <svg><use xlink:href="<%= asset_path('icons/people.svg#picture') %>" /></svg>
  <%-end-%>
</figure>
<%-end-%>
