// Use this file to require common dependencies or to setup useful test functions.

function clean_up(){
  $("section.notify").empty();
  $("section.workflow").empty();
  $("section.alert > ol").empty();
}